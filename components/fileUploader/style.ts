import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  fileInfo: {
    marginTop: 15,
    alignItems: "center"
  },
  fileName: {
    fontSize: 16,
    fontWeight: "bold"
  },
  fileSize: {
    fontSize: 16,
    color: "gray"
  },
  removeFile: {
    color: "red"
  }
});
