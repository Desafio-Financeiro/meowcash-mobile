import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    fontSize: 16
  },
  body: {
    paddingBottom: 15,
    paddingTop: 15,
    borderBottomWidth: 1,
    borderBottomColor: "#ccc",
  },
});
